config:
  target: "http://localhost:3345"
  phases:
    - duration: 10
      arrivalRate: 1
  processor: "./helpers.js"
scenarios:
  - name: "Create Trip Stub"
    flow:
    - post:
        beforeRequest: "getParams"
        url: "/api/trip"
        afterResponse: "printTrip"
